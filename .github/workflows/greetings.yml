name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.SUPER_TOKEN }}
        issue-message: 'First Issue!'' first issue'
        pr-message: 'First PR!'' first pr'
